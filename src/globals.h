#ifndef GLOBALS_H
#define GLOBALS_H

/** Place for common includes and *possibly* global variables (but thats baaad ok) */
#include <QObject>
#include <QDebug>
#include <QString>
#include <QSettings>

#endif // GLOBALS_H
